/* ── AI Suggested Fields ── */
.ai-suggested-field {
    background-color: #faf5ff !important;
    border-radius: 6px;
    transition: all 0.3s ease;
    box-shadow: 0 2px 8px rgba(138, 43, 226, 0.06);
}

/* Target MudBlazor's inner outlined border to fix double border bug */
.ai-suggested-field .mud-input-outlined-border {
    border: 1.5px dashed #c084fc !important;
}

/* Target label color when not focused */
.ai-suggested-field .mud-input-label {
    color: #8a2be2 !important;
}

/* Hover effect for AI fields */
.ai-suggested-field:hover .mud-input-outlined-border {
    border-color: #a855f7 !important;
}

/* Focus states */
.ai-suggested-field:focus-within {
    background-color: #ffffff !important;
}
.ai-suggested-field:focus-within .mud-input-outlined-border {
    border-style: solid !important;
    border-color: #8a2be2 !important;
    border-width: 2px !important;
}

.ai-suggestion-badge {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    background: linear-gradient(135deg, #f3e8ff 0%, #e9d5ff 100%);
    border: 1px solid #d8b4fe;
    color: #6b21a8;
    padding: 3px 8px;
    border-radius: 6px;
    font-size: 0.78rem;
    font-weight: 600;
    box-shadow: 0 2px 4px rgba(107, 33, 168, 0.04);
}

.ai-row-highlight {
    background-color: #faf5ff !important;
}

/* Apply the starting accent border only to the first cell of the row (fixes vertical borders in RTL) */
tr:has(> .ai-row-highlight) td:first-child {
    border-right: 4px solid #a855f7 !important;
}

/* ── Sidebar ── */
.wl-sidebar-wrap {
    height: calc(100vh - 88px);
    overflow: hidden;
    display: flex;
    flex-direction: column;
}

.wl-sidebar-head {
    padding: 14px 16px;
    border-bottom: 1px solid var(--mud-palette-divider);
    flex-shrink: 0;
}

.wl-list-scroll {
    flex: 1;
    overflow-y: auto;
    padding: 8px;
}

/* ── Patient card in sidebar ── */
.wl-pt-card {
    border-right: 4px solid transparent;
    border-radius: 8px;
    padding: 10px 12px;
    cursor: pointer;
    transition: background .15s, border-color .15s;
    margin-bottom: 5px;
}

.wl-pt-card:hover {
    background: var(--mud-palette-action-default-hover);
}

.wl-pt-card.is-sel {
    border-right-color: var(--mud-palette-primary) !important;
    background: var(--mud-palette-action-default-hover);
}

.wl-queue-num {
    width: 28px;
    height: 28px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 800;
    font-size: .76rem;
    flex-shrink: 0;
    color: #fff;
}

/* ── Patient header (always visible) ── */
.ph-card {
    border-bottom: 3px solid var(--mud-palette-primary);
}

.ph-vital {
    border: 1px solid var(--mud-palette-divider);
    border-radius: 8px;
    padding: 4px 6px;
    text-align: center;
    width: 82px;
    height: 82px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    overflow: hidden;
    flex-shrink: 0;
}

.ph-vital .vl {
    font-size: .7rem;
    color: var(--mud-palette-text-secondary);
    display: block;
    margin-bottom: 2px;
}

.ph-vital .vv {
    font-size: 1.05rem;
    font-weight: 700;
    display: block;
    line-height: 1.2;
    white-space: nowrap;
}

.ph-vital .vu {
    font-size: .7rem;
    color: var(--mud-palette-text-secondary);
    display: block;
    margin-top: 2px;
    min-height: 16px; /* To maintain spacing even if unit is missing */
}

/* ── Active visit animation ── */
.active-visit-card {
    border-right: 5px solid var(--mud-palette-success);
    background: rgba(76,175,80,.08);
    animation: pg 2s infinite;
}

@keyframes pg {
    0%, 100% {
        box-shadow: 0 0 0 rgba(76,175,80,.15);
    }

    50% {
        box-shadow: 0 0 18px rgba(76,175,80,.30);
    }
}

/* ── History cards ── */
.history-card {
    transition: transform .18s, box-shadow .18s;
}

.history-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 16px rgba(0,0,0,.12) !important;
}

/* ── Misc ── */
.exam-section-title {
    font-weight: 700;
    font-size: .95rem;
    display: flex;
    align-items: center;
    gap: 6px;
    margin-bottom: 10px;
}

.info-row-wrap {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    padding: 10px 16px;
}

.info-row-wrap .info-item {
    font-size: .875rem;
}

.info-row-wrap .info-item b {
    font-weight: 700;
}

/* ── Custom History Tabs ── */
.history-tabs .mud-tabs-toolbar {
    border-radius: 8px;
    padding: 4px;
}

.history-tabs .mud-tab {
    border-radius: 6px;
    margin: 0 4px;
    transition: all 0.3s ease;
    color: rgba(255, 255, 255, 0.7) !important;
}

.history-tabs .mud-tab .mud-icon-root {
    color: rgba(255, 255, 255, 0.7) !important;
}

.history-tabs .mud-tab-active {
    background-color: rgba(255, 255, 255, 0.25) !important;
    color: white !important;
    font-weight: bold;
    box-shadow: none;
}

.history-tabs .mud-tab-active .mud-icon-root {
    color: white !important;
}

/* ── Blue Nav Steps Container & White Active Tab Style ── */
.nav-steps-container {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    background-color: var(--mud-palette-primary);
    padding: 6px;
    border-radius: 10px;
    box-shadow: 0 4px 14px rgba(25, 118, 210, 0.25);
}

.step-nav-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    height: 38px;
    padding: 0 16px;
    border-radius: 8px;
    border: none;
    outline: none;
    background-color: transparent;
    color: #ffffff;
    font-size: 0.88rem;
    font-weight: 700;
    cursor: pointer;
    transition: all 0.2s ease-in-out;
    white-space: nowrap;
}

.step-nav-btn .mud-icon-root {
    color: #ffffff;
    transition: color 0.2s ease-in-out;
}

.step-nav-btn:hover:not(.is-active) {
    background-color: rgba(255, 255, 255, 0.18);
    color: #ffffff;
}

.step-nav-btn.is-active {
    background-color: #ffffff !important;
    color: var(--mud-palette-primary) !important;
    font-weight: 800;
    box-shadow: 0 3px 10px rgba(0, 0, 0, 0.18);
    transform: translateY(-1px);
}

.step-nav-btn.is-active .mud-icon-root {
    color: var(--mud-palette-primary) !important;
}

/* ── Full Screen Cards ── */
.waiting-card {
    transition: transform 0.2s, box-shadow 0.2s;
    height: 100%;
    border-radius: 12px;
    border-top: 4px solid var(--mud-palette-primary);
}

.waiting-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 24px rgba(0,0,0,0.15) !important;
    border-top-color: var(--mud-palette-secondary) !important;
}

.patient-details-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 16px;
    font-size: 0.85rem;
}

.detail-item {
    display: flex;
    flex-direction: column;
    overflow: hidden;
}

.detail-label {
    color: var(--mud-palette-text-secondary);
    font-size: 0.75rem;
}

.detail-value {
    font-weight: 600;
    color: var(--mud-palette-text-primary);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

/* ── Full Width Live Audio Spectrum Visualizer Container (Supports Light & Dark Theme) ── */
.full-width-eq-container {
    width: 100%;
    background-color: var(--mud-palette-background-grey, rgba(0, 0, 0, 0.03));
    border: 1px solid var(--mud-palette-lines-default, rgba(0, 0, 0, 0.12));
    border-radius: 12px;
    box-shadow: inset 0 2px 6px rgba(0, 0, 0, 0.04);
    transition: background-color 0.3s ease, border-color 0.3s ease;
    overflow: hidden;
}

/* ── Live Sound Equalizer Visualizer ── */
.audio-equalizer-wrapper {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    padding: 5px 14px;
    background: linear-gradient(135deg, rgba(239, 68, 68, 0.08) 0%, rgba(168, 85, 247, 0.08) 100%);
    border: 1px solid rgba(239, 68, 68, 0.25);
    border-radius: 20px;
    box-shadow: 0 4px 15px rgba(239, 68, 68, 0.1);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    opacity: 0;
    transform: scale(0.92);
    max-width: 0;
    overflow: hidden;
    pointer-events: none;
}

.audio-equalizer-wrapper.is-active {
    opacity: 1;
    transform: scale(1);
    max-width: 320px;
    pointer-events: auto;
}

.eq-pulse-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background-color: #ef4444;
    box-shadow: 0 0 10px #ef4444;
    animation: eq-pulse 1.2s ease-in-out infinite;
    flex-shrink: 0;
}

@keyframes eq-pulse {
    0%, 100% {
        transform: scale(0.85);
        opacity: 0.7;
        box-shadow: 0 0 4px #ef4444;
    }
    50% {
        transform: scale(1.25);
        opacity: 1;
        box-shadow: 0 0 12px #ef4444;
    }
}

.eq-bars {
    display: flex;
    align-items: center;
    gap: 3px;
    height: 24px;
}

.eq-bar {
    width: 3.5px;
    height: 6px;
    border-radius: 4px;
    background: linear-gradient(180deg, #ef4444 0%, #a855f7 100%);
    box-shadow: 0 2px 6px rgba(239, 68, 68, 0.3);
    transition: height 0.3s ease, background 0.3s ease;
}

.audio-equalizer-wrapper.is-active .eq-bar {
    animation: eq-wave 1.1s ease-in-out infinite alternate;
}

.eq-bar:nth-child(1)  { animation-duration: 0.7s;  animation-delay: 0.05s; }
.eq-bar:nth-child(2)  { animation-duration: 1.0s;  animation-delay: 0.25s; }
.eq-bar:nth-child(3)  { animation-duration: 0.6s;  animation-delay: 0.12s; }
.eq-bar:nth-child(4)  { animation-duration: 1.2s;  animation-delay: 0.40s; }
.eq-bar:nth-child(5)  { animation-duration: 0.8s;  animation-delay: 0.18s; }
.eq-bar:nth-child(6)  { animation-duration: 0.95s; animation-delay: 0.32s; }
.eq-bar:nth-child(7)  { animation-duration: 1.15s; animation-delay: 0.08s; }
.eq-bar:nth-child(8)  { animation-duration: 0.65s; animation-delay: 0.22s; }
.eq-bar:nth-child(9)  { animation-duration: 1.05s; animation-delay: 0.35s; }
.eq-bar:nth-child(10) { animation-duration: 0.75s; animation-delay: 0.15s; }
.eq-bar:nth-child(11) { animation-duration: 0.9s;  animation-delay: 0.28s; }
.eq-bar:nth-child(12) { animation-duration: 1.1s;  animation-delay: 0.04s; }

@keyframes eq-wave {
    0% {
        height: 4px;
        opacity: 0.4;
        transform: scaleY(1);
    }
    50% {
        height: 22px;
        opacity: 1;
        transform: scaleY(1.1);
    }
    100% {
        height: 7px;
        opacity: 0.7;
        transform: scaleY(0.9);
    }
}

.eq-text {
    font-size: 0.78rem;
    font-weight: 700;
    color: #dc2626;
    white-space: nowrap;
    letter-spacing: 0.2px;
}

@keyframes mic-pulse {
    0%, 100% { transform: scale(1); opacity: 1; }
    50% { transform: scale(1.2); opacity: 0.8; }
}
